import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        backgroundColor: '#673738',
        width: 500,
        height: 500
    }
});

export default styles;