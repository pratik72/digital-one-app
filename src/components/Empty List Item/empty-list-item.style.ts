import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    listView: {
        paddingVertical: 15
    },
    text: {
        textAlign: 'center',
        fontSize: 16
    }
});

export default styles;